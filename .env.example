# Example environment variables for dflow-traefik
# Replace the values as needed for your environment

# The wildcard domain for routing (e.g., example.com)
WILD_CARD_DOMAIN=yourdomain.com

# JWT token for authentication (set a secure value)
JWT_TOKEN=your_jwt_token_here

# (Optional) Port to run the server on (default: 3000)
PORT=3000
